<% if can? :edit, @page %>
	<%= link_to 'Edit', edit_page_path(@page) %> |
	<%= link_to 'Back', pages_path %>
<% end %>

<h2> <%= @page.name %> </h2>
<div id="pages">
	<%= @page.content.html_safe %>
</div>
