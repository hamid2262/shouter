<div id="pages">

	<% if can? :edit, @page %>
		<%= link_to 'Edit', edit_page_path(@page) %> |
		<%= link_to 'Back', pages_path %>
	<% end %>

	<h1> <%= @page.name %> </h1>
	<div id="content">
		<%= @page.content.html_safe %>
	</div>
</div>
