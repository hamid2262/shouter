<div class="col-md-4">
	<%= render partial: 'users/users_raw', locals: {users: @followed_users, title: "Your Followed Users "} %>
	<%= render partial: 'users/users_raw', locals: {users: @random_users, title: "Users you may want to know"} %>
</div>

<div class="col-md-8">
	<%= render 'shouts/form' %>
	<%= render @dashboard.timeline %>
</div>