<%= render 'head' %>

<%= render 'navbar' %>

<% if current_user.try(:is_admin?) %>
	<%= render 'admin_navbar' %>		
<% end %>

<div id="wrap" class="container">
		
	<%= render 'header' %>

	<%#= render 'searches/form' %>
  <div id="siteContent">
    <%= flash_creator flash %>
    <%= yield %>  
  </div>	  
</div>
<%= render 'footer' %>