<%= render 'shared_mail/style' %>
<%= render 'shared_mail/header' %>

<table>
	<tbody>
		<tr>
			<th>آی دی: </th>
			<td><%= @user.id %></td>
		</tr>
		<tr>
			<th>نام: </th>
			<td><%= link_to @user.name, url_for(	
							host: @host,
							controller: 'profiles', 
							action: 'show', 
							id: @user.slug, 
							locale: 'fa') %>
			</td>
		</tr>
		<tr>
			<th>زمان ثبت نام: </th>
			<td><%= @jalali_time %></td>
		</tr>
		<tr>
			<th>تاریخ ثبت نام: </th>
			<td>
				<%= @jalali_day %> ,
				<%= @jalali_month %>,
				<%= @jalali_day_num %> 
			</td>
		</tr>		
	</tbody>
</table>
	
<h4>دعوت کننده ها: </h4>
<ol>
	<% while	@user.inviter %>
		<li>
			<%= link_to @user.inviter.name, url_for(	
				host: @host,
				controller: 'profiles', 
				action: 'show', 
				id: @user.inviter.slug, 
				locale: 'fa') %>
		</li>
		<% @user = @user.inviter %>
	<% end %>
</ol>

<%= render 'shared_mail/footer' %>
