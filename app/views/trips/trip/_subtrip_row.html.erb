<tr>
	<th><%= link_to(subtrip.origin.local_name, subtrip_path(subtrip.id)) %></th>
	<th><%= link_to(subtrip.destination.local_name, subtrip_path(subtrip.id)) %></th>
	<td><%= link_to( jalali_time(subtrip.date_time), subtrip_path(subtrip.id)) %></td>
	<th><%= link_to(t('currency', amount: subtrip.price), subtrip_path(subtrip.id)) %></th>
</tr>
