<% if current_user && current_user.is_admin? %>
	<% current = user %>
<% else %>
	<% current = current_user %>
<% end %>

<div class="panel panel-default clear">
  <div class="panel-heading">
  	<%= user.name %> 
  	<%=t  '.overview'  %>
		<% if current_user && current_user.can_follow?(user) %>
			<div class="follow pull-<%= lang_other_side %>"><%= follow_button user %></div> 
		<% end %>
  </div>
 
	<% if current_user %>
		<table class="table">
			<tbody>
				<tr>
					<th><%= t(".last_activity") %></th>
					<th><%= resent_activity(user, "activity") %></th>
				</tr>
				<tr>
					<th><%= t(".number_of_followers", user: user.name) %></th>
					<th><%= user.followers.size %></th>
				</tr>
				<tr>
					<th><%= t(".number_of_followed_users", user: user.name) %></th>
					<th><%=  user.followed_users.size %></th>
				</tr>
				<tr>
					<th><%= link_to(t(".number_of_network", user: user.name), network_path(current.slug) ) %></th>
					<th><%= Network.network_size(user) %></th>
				</tr>
				<tr>
					<th><%= t(".trips_count") %></th>
					<th><%= @trips_count %></th>
				</tr>
				<tr>
					<th><%= t(".bookings_count") %></th>
					<th><%= @bookings_count %></th>
				</tr>
				<tr>
					<th><%= t(".trip_canceled_count") %></th>
					<th><%= @trip_canceled_count %></th>
				</tr>
				<tr>
					<th><%= t(".booking_canceled_count") %></th>
					<th><%= @booking_canceled_count %></th>
				</tr>
			</tbody>
		</table>

		<% if user.slug_updated %>
			<table class="table">
				<tbody>
					<tr>
						<th><%= t(".url") %></th>
						<th><%=  link_to_profile(user) %></th>
					</tr>
				</tbody>
			</table>
		<% end %>
	<% else %>
		<%= force_to_sign_in_message %>
	<% end %> <!-- if current_user -->
			
</div>

